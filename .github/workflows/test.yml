name: Test binary-install

on: [pull_request]

jobs:
  binary-install:
    name: Test binary-install
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Run tests
        run: lerna run test
